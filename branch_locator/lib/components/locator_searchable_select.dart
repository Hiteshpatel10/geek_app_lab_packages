import 'package:flutter/material.dart';

class LocatorSearchableSelect extends StatelessWidget {
  const LocatorSearchableSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
